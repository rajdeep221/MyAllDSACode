package IGT;

public class Main {
    public static void main(String[] args) {
        // int v = 4;
        // CreationGraph ob = new CreationGraph(v);

        // int arr1[] = { 0, 1, 1, 2, 2, 2, 3, 3 };
        // int arr2[] = { 2, 2, 3, 0, 1, 3, 1, 2 };
        // int arr3[] = { 2, 10, 0, 2, 10, -1, 0, -1 };
        // for (int i = 0; i < arr1.length; i++) {
        // ob.CreateGraph(arr1[i], arr1[i], arr2[i], arr3[i]);
        // }
        // ob.printNeighbout(2);
        // ob.primsAlgo();

        // Graph ob1 = new Graph(v);
        // for (int i = 0; i < arr1.length; i++) {
        // ob1.createGraph(arr1[i], arr1[i], arr2[i]);
        // }

        // boolean vis[] = new boolean[v];
        // ob1.dfs(vis, 0);
        // System.out.println(vis[3]);
        // System.out.println(ob1.noOfPahts(0, 4));
    }
}
